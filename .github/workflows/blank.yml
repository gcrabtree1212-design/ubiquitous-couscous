git clone https://github.com/gcrabtree1212-design/ubiquitous-couscous.git || (cd ubiquitous-couscous && git fetch origin); cd ubiquitous-couscous || exit 1; git fetch origin; (git checkout -B add/ci-and-community-files || git checkout add/ci-and-community-files); mkdir -p .github/workflows src; cat > README.md <<'EOF'
# Ubiquitous Couscous

[![CI](https://github.com/gcrabtree1212-design/ubiquitous-couscous/actions/workflows/ci.yml/badge.svg?branch=main)](https://github.com/gcrabtree1212-design/ubiquitous-couscous/actions/workflows/ci.yml)

Short project description — replace this with an overview of the repository and what problem it solves.

Quick start
1. Clone:
   - SSH: git clone git@github.com:gcrabtree1212-design/ubiquitous-couscous.git
   - HTTPS: https://github.com/gcrabtree1212-design/ubiquitous-couscous.git
2. Run the sample:
   python3 src/main.py

Structure
- src/           — application source (example: src/main.py)
- .github/       — CI workflow
- README.md
- LICENSE
- .gitignore

Contributing
See CONTRIBUTING.md for contribution guidelines.

Code of Conduct
This project follows a Code of Conduct. See CODE_OF_CONDUCT.md.

License
MIT — see LICENSE.
EOF
cat > .github/workflows/ci.yml <<'EOF'
name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest flake8

      - name: Run flake8
        run: |
          flake8

      - name: Run tests
        run: |
          pytest -q
EOF
cat > CONTRIBUTING.md <<'EOF'
# Contributing

Thank you for your interest in contributing to this project!

How to contribute
1. Fork the repository (or create a branch if you have push access).
2. Create a branch: git checkout -b feature/my-feature
3. Make changes and add tests where applicable.
4. Run lint and tests locally:
   - pip install -r requirements.txt    # if present
   - pip install pytest flake8
   - flake8
   - pytest

PR checklist
- Tests added/updated when applicable.
- Code follows style and linting.
- Clear PR description with motivation and links to issues.
EOF
cat > CODE_OF_CONDUCT.md <<'EOF'
# Contributor Covenant Code of Conduct

This project adopts the Contributor Covenant Code of Conduct (v2.1). By participating, you agree to abide by its terms.

Summary
- Be inclusive and respectful.
- No harassment or discriminatory language.
- Report unacceptable behavior to the maintainers.

For the full text see: https://www.contributor-covenant.org/version/2/1/code_of_conduct/
EOF
git add README.md .github/workflows/ci.yml CONTRIBUTING.md CODE_OF_CONDUCT.md || true; git commit -m "Add CI workflow and community files" || echo "No changes to commit"; git push -u origin add/ci-and-community-files
